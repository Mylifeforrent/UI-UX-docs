# 视觉层级

视觉层级回答一个问题：用户打开页面后，第一眼、第二眼和下一次操作分别是什么。

## 可执行规则

- 一个页面只保留一个主要任务，主按钮的文案使用动作加对象。
- 用尺寸、字重、位置和留白区分标题、摘要、正文、辅助信息。
- 同一层级使用同一种表达，不要让每个模块都抢主视觉。
- 通过对齐线和间距组表达关系，少用边框和装饰分割。
- 先画灰度线框，能验证结构后再加入颜色和品牌。

## 练习

截图一个企业页面，标记主要任务、次要任务、数据状态和风险提示。将所有元素按“必须看、应该看、需要时看”分组。
